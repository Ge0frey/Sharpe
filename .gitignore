.anchor
.DS_Store
target
**/*.rs.bk
node_modules
test-ledger
.yarn
.surfpool

# Local credentials — never commit.
# .state.json holds a guest JWT + TxLINE api token; .taker.json is a raw keypair.
scripts/.state.json
scripts/.taker.json
scripts/*.json.bak
